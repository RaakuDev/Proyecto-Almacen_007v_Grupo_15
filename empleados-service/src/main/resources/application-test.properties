spring.application.name=empleados-service
server.port=8091

# Conexion PostgreSQL TEST
spring.datasource.url=jdbc:postgresql://localhost:5457/empleados_test_db
spring.datasource.username=admin
spring.datasource.password=admin123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA para pruebas
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Flyway desactivado para pruebas
spring.flyway.enabled=false

# URL de microservicios TEST
ventas-service.url=http://localhost:8088